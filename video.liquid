{%- if section.settings.video-3d != blank -%}
    {{- section.settings.video-3d | video_tag: autoplay: true, playslinline: true, muted: true, loop: true, preload: 'metadata' -}}
{%- endif -%}


  {% schema %}
    {
    "name": "Review",
    "settings": [
        {
          "type": "video",
          "id": "video-3d",
          "label": "3d Video"
        }
    ],
    "blocks": [],
    "presets": [
    {
    "name": "Review"
    }
    ]
    }
    {% endschema %}